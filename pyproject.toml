[project]
name = "code-rag"
version = "0.1.0"
description = "Thin MCP layer for HyperGraph code-rag stack"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = []

[build-system]
requires = ["uv_build>=0.9.26"]
build-backend = "uv_build"
